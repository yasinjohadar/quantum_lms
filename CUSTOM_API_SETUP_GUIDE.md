# دليل إعداد Custom API Provider للواتساب

## 📍 أين تضع API Key

### الخطوات:

1. **اذهب إلى صفحة الإعدادات:**
   ```
   /admin/whatsapp-settings
   ```

2. **في قسم "اختيار المزود (Provider)":**
   - اختر **"Custom API Provider"** من القائمة المنسدلة

3. **ستظهر لك حقول "إعدادات Custom API":**

   **أ) رابط API (API URL):** ⭐ مطلوب
   - ضع رابط endpoint الخاص بالوسيط
   - مثال: `https://api.example.com/whatsapp/send`
   - أو: `https://api.provider.com/v1/messages`

   **ب) API Key:** ⭐ مطلوب (يتم تشفيره تلقائياً)
   - ضع رمز API الذي أعطاك إياه الوسيط
   - مثال: `sk_live_1234567890abcdef`
   - أو أي رمز API أعطاك إياه الوسيط

   **ج) طريقة الطلب (Method):**
   - اختر `POST` (الأكثر استخداماً)
   - أو `GET` إذا كان API يطلب ذلك

   **د) Headers إضافية (اختياري):**
   - إذا كان API يطلب headers إضافية
   - مثال:
     ```json
     {
       "X-Custom-Header": "value",
       "Authorization": "Bearer your-token"
     }
     ```
   - إذا كان API Key يُرسل في Header، لا حاجة لإضافته هنا (سيضاف تلقائياً)

---

## 📝 مثال عملي:

إذا أعطاك الوسيط:
- **API Key:** `abc123xyz789`
- **API URL:** `https://api.whatsapp-provider.com/send`

### الإعدادات:
1. **Provider:** Custom API Provider
2. **رابط API:** `https://api.whatsapp-provider.com/send`
3. **API Key:** `abc123xyz789`
4. **Method:** POST
5. **Headers:** `{}` (فارغ إذا لم يطلب الوسيط headers إضافية)

---

## 🔍 ملاحظات مهمة:

### 1. API Key يتم تشفيره تلقائياً
- لا تقلق، API Key سيتم تشفيره في قاعدة البيانات
- عند التعديل، اتركه فارغاً للاحتفاظ بالقيمة القديمة

### 2. تنسيق Request
- النظام يرسل request بهذا التنسيق:
  ```json
  {
    "to": "+1234567890",
    "message": "نص الرسالة",
    "type": "text"
  }
  ```
- أو للـ Templates:
  ```json
  {
    "to": "+1234567890",
    "template": "template_name",
    "language": "ar",
    "type": "template"
  }
  ```

### 3. إذا كان API يطلب تنسيق مختلف
- قد تحتاج إلى تعديل `CustomApiProvider` ليتماشى مع تنسيق API الخاص بك
- أو تواصل مع مطور النظام

### 4. اختبار الاتصال
- بعد إدخال البيانات، اضغط **"اختبار الاتصال"**
- سيحاول النظام الاتصال بـ API للتحقق من صحة البيانات

---

## ⚠️ استكشاف الأخطاء:

### الخطأ: "API URL مطلوب"
- تأكد من إدخال رابط API بشكل صحيح
- يجب أن يبدأ بـ `http://` أو `https://`

### الخطأ: "فشل الاتصال"
- تحقق من صحة API URL
- تحقق من صحة API Key
- تأكد من أن API يستجيب على `/health` endpoint
- أو أن API URL الرئيسي يستجيب

### الخطأ: "حدث خطأ"
- تحقق من logs في `storage/logs/whatsapp.log`
- قد يكون هناك مشكلة في تنسيق request

---

## 📞 مثال كامل:

**إذا كان لديك:**
- Provider: شركة XYZ
- API Key: `xyz_api_key_12345`
- API URL: `https://xyz-api.com/whatsapp/message`

**ضع في الإعدادات:**
1. ✅ Provider: **Custom API Provider**
2. ✅ API URL: **https://xyz-api.com/whatsapp/message**
3. ✅ API Key: **xyz_api_key_12345**
4. ✅ Method: **POST**
5. ✅ Headers: **{}** (فارغ)

**ثم:**
- اضغط "اختبار الاتصال" للتأكد
- اضغط "حفظ الإعدادات"

---

**ملاحظة:** إذا كان الوسيط يطلب تنسيق request مختلف أو headers خاصة، قد تحتاج إلى تعديل `CustomApiProvider` class.

