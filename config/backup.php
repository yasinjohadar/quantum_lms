<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Webhook Notification URL
    |--------------------------------------------------------------------------
    |
    | يُستدعى بعد اكتمال/فشل أي نسخة احتياطية (BackupNotificationService).
    | اتركه فارغاً لتعطيل إشعارات الـ webhook.
    |
    */
    'webhook_url' => env('BACKUP_WEBHOOK_URL'),

    /*
    |--------------------------------------------------------------------------
    | Job Timeout
    |--------------------------------------------------------------------------
    |
    | مهلة CreateBackupJob بالثواني — يجب أن تتسع لأكبر نسخة متوقعة (كاملة/قاعدة بيانات).
    |
    */
    'job_timeout' => (int) env('BACKUP_JOB_TIMEOUT', 3600),

    /*
    |--------------------------------------------------------------------------
    | SQL Dump Chunk Size
    |--------------------------------------------------------------------------
    |
    | عدد الصفوف لكل عبارة INSERT عند تفريغ قاعدة البيانات في BackupService.
    |
    */
    'sql_dump_chunk_size' => (int) env('BACKUP_SQL_DUMP_CHUNK_SIZE', 100),

    /*
    |--------------------------------------------------------------------------
    | SQL Dump Max Statement Bytes
    |--------------------------------------------------------------------------
    |
    | الحد الأقصى (بايت) لحجم عبارة INSERT الواحدة عند تفريغ قاعدة البيانات.
    | يُنهي الدفعة الحالية عند تجاوز هذا الحد حتى لو لم يصل عدد الصفوف إلى
    | sql_dump_chunk_size — يحمي من خطأ MySQL "Got a packet bigger than
    | 'max_allowed_packet' bytes" عند وجود أعمدة كبيرة (نص/HTML طويل مثلاً)
    | حيث لا يكفي تحديد عدد الصفوف وحده. الافتراضي (512 كيلوبايت) أقل بأمان
    | من أي إعداد معقول لـ max_allowed_packet على الخادم.
    |
    */
    'sql_dump_max_statement_bytes' => (int) env('BACKUP_SQL_DUMP_MAX_STATEMENT_BYTES', 512 * 1024),

    /*
    |--------------------------------------------------------------------------
    | Excluded Tables
    |--------------------------------------------------------------------------
    |
    | جداول تُستبعد تماماً (بلا DROP/CREATE ولا بيانات) من أي نسخة قاعدة بيانات.
    | السبب: هذه جداول تتبّع النسخ الاحتياطية نفسها أو حالة تشغيلية عابرة —
    | تضمينها يسبّب تناقضاً ذاتياً عند الاستعادة (استعادة نسخة تستبدل جدول
    | backups بصورة قديمة تعود للحظة إنشاء تلك النسخة نفسها، فتظهر النسخة التي
    | استعدتها للتو وكأنها فشلت)، وقد تُعيد جلسات/مهام قديمة غير صالحة للحياة.
    | يبقى تسجيل هذه الجداول في قاعدة البيانات الحية دون أي تأثير من الاستعادة.
    |
    */
    'excluded_tables' => [
        'backups',
        'backup_logs',
        'backup_schedules',
        'sessions',
        'jobs',
        'job_batches',
        'failed_jobs',
        'cache',
        'cache_locks',
    ],

    /*
    |--------------------------------------------------------------------------
    | Schedule Lock Duration
    |--------------------------------------------------------------------------
    |
    | مدة قفل Cache (بالثواني) لمنع تشغيل نفس الجدولة أو دورة الجدولة العامة
    | أكثر من مرة في آن واحد (BackupScheduleService).
    |
    */
    'schedule_lock_seconds' => (int) env('BACKUP_SCHEDULE_LOCK_SECONDS', 3600),

    /*
    |--------------------------------------------------------------------------
    | Stuck Job Timeout
    |--------------------------------------------------------------------------
    |
    | عدد الدقائق التي تُعتبر بعدها نسخة بحالة running/pending "عالقة" —
    | يستخدمها أمر backup:reconcile-stuck لتحويلها إلى failed تلقائياً.
    |
    */
    'stuck_job_timeout_minutes' => (int) env('BACKUP_STUCK_JOB_TIMEOUT_MINUTES', 90),

    /*
    |--------------------------------------------------------------------------
    | Stuck Pending Timeout
    |--------------------------------------------------------------------------
    |
    | عدد الدقائق التي تُعتبر بعدها نسخة بحالة pending "لم يلتقطها الطابور" —
    | مهلة أقصر من stuck_job_timeout_minutes لأن pending يعني الـ job لم يبدأ أصلاً.
    |
    */
    'stuck_pending_timeout_minutes' => (int) env('BACKUP_STUCK_PENDING_TIMEOUT_MINUTES', 30),

    /*
    |--------------------------------------------------------------------------
    | Encryption at Rest (غير مفعّل حالياً)
    |--------------------------------------------------------------------------
    |
    | ملاحظة: أرشيفات النسخ من نوع full/config تتضمن ملف .env الحقيقي (أسرار ومفاتيح API)
    | وتُخزَّن حالياً بدون تشفير على مستوى الملف نفسه (فقط بيانات اعتماد التخزين مشفّرة
    | عبر AppStorageConfig::config). التحسين الموصى به لاحقاً: تشفير AES-256 لاحق للضغط
    | باستخدام مفتاح مخصص BACKUP_ENCRYPTION_KEY منفصل تماماً عن APP_KEY (حتى لا يفقد أي
    | تدوير مستقبلي لـ APP_KEY القدرة على فك تشفير نسخ قديمة). أُجِّل تنفيذ هذا بقرار صريح
    | من المستخدم في هذه الجولة.
    |
    */
    'encryption' => [
        'enabled' => false,
    ],

];
