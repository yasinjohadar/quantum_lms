# تقرير فحص نظام التحفيز (Gamification) — أكاديمية كوانتم

فحص شامل لجانبَي الأدمن والطالب: هل يعمل النظام بشكل صحيح، وما التحسينات المقترحة.
التاريخ: 2026-07-21

---

## الملخص التنفيذي

النظام **مبنيّ بشكل جيد من ناحية البنية**: قاعدة بيانات سليمة (مفاتيح أجنبية، فهارس، قيود تفرّد على جداول ربط المستخدم)، بذور بيانات جاهزة (مستويات/شارات/إنجازات/مهام تُنشأ تلقائياً)، وفصل نظيف بين المحرّك (Services) والواجهات. لكنه **لا يعمل بشكل صحيح بالكامل**، وفيه عدد من الأعطال الحرجة التي تجعل أجزاءً رئيسية إمّا معطّلة تماماً أو قابلة للاستغلال.

الحكم العام: **يعمل جزئياً**. النقاط الأساسية (اختبارات/دروس/أسئلة) تُمنح فعلاً وتظهر في الهيدر ولوحة الطالب، لكن:

- **ثغرة حرجة**: يمكن للطالب "زراعة" نقاط غير محدودة من الدروس (لا توجد حماية من التكرار عند منح النقاط).
- **التحديات (Challenges) معطّلة تماماً** — كود ميت لا يُستدعى من أي مكان.
- **المهام اليومية/الأسبوعية لا تُعاد تهيئتها إطلاقاً** (الأوامر موجودة لكنها غير مجدولة) → كل مهمة تُنجَز مرة واحدة فقط مدى الحياة.
- **لوحات المتصدرين (أسبوعي/شهري/حسب المادة) تُطلق خطأ فادحاً (500)** بسبب import مفقود.
- **جزء من إعدادات النقاط في لوحة الأدمن لا أثر له** (مفاتيح لا تطابق أنواع الأحداث).

هذه المشاكل قابلة للإصلاح، ومعظمها بتعديلات محدودة. التفاصيل أدناه مرتّبة حسب الخطورة.

---

## أولاً: ما الذي يعمل بشكل صحيح

- **منح النقاط من قاعدة البيانات**: `PointService::calculatePoints` يقرأ قيم النقاط من `SystemSetting` (المفتاح `gamification_points_{type}`) مع قيم افتراضية منطقية عند غياب الإعداد. مسار النقاط للاختبارات والدروس والأسئلة يعمل. (`app/Services/PointService.php:16-77`)
- **مكافأة الدرجة الكاملة في الاختبار** (نسبة ≥ 100%) تُضاف فعلاً. (`PointService.php:63-67`)
- **الأحداث المُوصّلة فعلياً بالمحرّك**: إكمال الاختبار (`StudentQuizController.php:392`)، حضور/إكمال الدرس (`StudentLessonController.php:570-587`)، الإجابة على سؤال (`QuestionAttemptService.php:111`)، مشاهدة/تنزيل عنصر مكتبة (`StudentLibraryController.php:70,128`).
- **منع إعادة إنهاء نفس محاولة الاختبار/السؤال**: محميّ بشرط `status !== 'in_progress'`، فلا تُطلق المحاولة الواحدة المحرّك أكثر من مرة.
- **الشارات محميّة من التكرار** عبر فحص `exists()` مسبق وإعادة فحص داخل `awardBadge`. (`BadgeService.php:34,105-111`)
- **خصم نقاط المكافأة داخل معاملة قاعدة بيانات** (DB transaction). (`RewardService.php:44-69`)
- **البنية والمخطط سليمان**: كل الجداول (17) لها مفاتيح أجنبية مع cascade، وقيود تفرّد صحيحة على `user_badges` و`user_achievements` و`user_challenges` و`user_levels` و`leaderboard_entries`، وفهارس على معظم المسارات الساخنة.
- **بذور البيانات كاملة وموصولة**: `DatabaseSeeder → GamificationSettingsSeeder + GamificationSeeder` تنشئ المستويات والشارات والإنجازات والمهام تلقائياً — لا حاجة لإنشاء كل شيء يدوياً.
- **الصلاحيات الخشنة**: كل مسارات الأدمن محميّة بـ `['auth','check.user.active','admin']`، فلا وصول عام.
- **حذف ناعم (Soft delete) متّسق**: جداول التعريفات تستخدم SoftDeletes، وجداول الأحداث/الربط تُحذف حذفاً صلباً — وهو التقسيم الصحيح.

---

## ثانياً: الأخطاء الحرجة (يجب إصلاحها فوراً)

### 1. زراعة نقاط غير محدودة من الدروس (ثغرة أمان/نزاهة) — حرج
`markLessonStatus` يستخدم `updateOrCreate` لسجل الإكمال (سليم)، لكنه يستدعي `processLessonAttendance`/`processLessonCompletion` في **كل طلب** دون التحقق أنها المرة الأولى. و`awardPoints` **لا يحتوي أي حماية من التكرار** ويُدرج معاملة نقاط جديدة في كل مرة (المصدر يُمرَّر `null`). بذلك يستطيع الطالب إرسال `status=attended` مراراً وكسب 10 نقاط كل مرة بلا حد.
- الملفات: `StudentLessonController.php:570-587`، `GamificationService.php:34`، `PointService.php:82-102`.
- الإصلاح: منح النقاط فقط عند **أول** انتقال إلى attended/completed؛ وتخزين `source_type/source_id` على المعاملة مع فهرس تفرّد على `(user_id, type, source_type, source_id)` واستخدام `firstOrCreate`.

### 2. سباق استرداد المكافآت + كاش قديم = صرف مزدوج ورصيد سالب — حرج
`claimReward` يفحص الرصيد و`is_available` **خارج** المعاملة، بلا `lockForUpdate` على صف المكافأة وبلا قيد تفرّد على `user_rewards`. والأسوأ أن فحص الرصيد يستعمل `getUserTotalPoints` **المُخزّن مؤقتاً 120 ثانية**، فطلبان متتاليان يقرآن نفس الرصيد المخزَّن، ويمرّان معاً، ويخصمان معاً.
- الملفات: `RewardService.php:34-69`، `PointService.php:107-115`.
- الإصلاح: لفّ كل العملية في `DB::transaction` مع `lockForUpdate()` على المكافأة، وإعادة قراءة الرصيد من قاعدة البيانات (تجاوز الكاش) داخل القفل، وقيد تفرّد لمنع المطالبة المزدوجة، وزيادة المخزون ذرّياً عبر `where('quantity_claimed','<','quantity_available')->increment()`.

### 3. التحديات (Challenges) كود ميت — حرج (ميزة معطّلة كلياً)
`ChallengeService::checkChallengeProgress` و`completeChallenge` موجودان لكن **لا يُستدعيان من أي مكان** (البحث في كامل المستودع لا يُظهر سوى التعريف). لا يوجد مسار "الانضمام لتحدٍّ"، و`processEvent` لا يمسّ التحديات إطلاقاً، و`UserChallenge.progress` لا يُحدَّث أبداً، ونقاط التحديات لا تُمنح. صفحة التحديات للطالب تعرض قائمة فقط بلا أي تفاعل فعلي.
- الإصلاح: توصيل التحديات (زر انضمام + استدعاء `checkChallengeProgress` من `processEvent` أو أمر مجدول لإعادة الحساب)، أو إزالة الواجهة/الخدمة الميتة لتجنّب التضليل.

### 4. لوحات المتصدرين (أسبوعي/شهري/حسب المادة) تُطلق خطأ فادحاً 500 — حرج
`LeaderboardService` يستعمل `PointTransaction` في السطرين 113 و130 لكنه **لا يستورده** (الـ imports في الأعلى تُغفله)، فيحلّه PHP إلى `App\Services\PointTransaction` غير الموجود → `Class not found` عند تحديث أي لوحة أسبوعية/شهرية/حسب مادة. فقط النوع `global` يعمل.
- الملف: `app/Services/LeaderboardService.php` (ينقص `use App\Models\PointTransaction;`).
- الإصلاح: إضافة الـ import سطر واحد.

### 5. المهام اليومية/الأسبوعية لا تُعاد تهيئتها إطلاقاً — حرج
الأوامر `tasks:reset-daily` و`tasks:reset-weekly` **موجودة** (`app/Console/Commands/`) لكنها **غير مسجّلة في المجدول** (`routes/console.php` يجدول النسخ الاحتياطي فقط). كما أن `UserTask` مفتاحه `(user, taskable_type, taskable_id)` بلا حقل تاريخ/فترة، فمتى اكتملت مهمة بقيت مكتملة للأبد، و`firstOrCreate` يُعيد نفس الصف "المنتهي" دون إعادته إلى `pending`. النتيجة: كل مهمة قابلة للإنجاز **مرة واحدة فقط مدى الحياة**، ولا شيء يعيد تهيئتها.
- الملفات: `TaskService.php:91-101,184-221`، `routes/console.php`.
- الإصلاح: إضافة عمود `period_date` (أو معرّف أسبوع) إلى `user_tasks` مع قيد تفرّد يشمله، وتسجيل الأوامر في المجدول، وجعل إعادة التهيئة والإنشاء عملية ذرّية واحدة.

### 6. جزء من إعدادات النقاط في لوحة الأدمن بلا أثر — حرج (تضليل إداري)
شاشة الإعدادات تحفظ `points[attendance]` → المفتاح `gamification_points_attendance`، بينما مسار الحضور يقرأ `gamification_points_lesson_attended`؛ فقيمة "الحضور" التي يضبطها الأدمن **لا تُطبَّق أبداً** (تعود دوماً إلى 10). ونفس الأمر لـ `points[course_completed]` — لا يوجد `calculatePoints('course_completed')` في أي مكان.
- الملفات: `settings.blade.php:48,78`، `GamificationService.php:63`، `PointService.php`.
- الإصلاح: مطابقة مفاتيح النموذج مع أنواع الأحداث (`lesson_attended`)، وربط `course_completed` بحدث حقيقي أو إزالته.

---

## ثالثاً: أخطاء متوسطة

### 7. لوحة "النقاط حسب النوع" لدى الطالب تعرض أصفاراً
`GamificationController::stats` يستعلم عن أنواع `attendance|lesson_completion|quiz|question`، لكن المعاملات تُخزَّن بأسماء الأحداث (`lesson_attended`, `lesson_completed`, `quiz_completed`, `question_answered`). المفاتيح لا تتطابق فكل فئة تُرجع صفراً. (`GamificationController.php:171-176`، `GamificationService.php:34`)

### 8. المجاميع المخزّنة في `UserLevel` تتأخّر (قيمة قديمة)
في `awardPoints` الترتيب: إدراج المعاملة → `updateUserTotalPoints()` (يقرأ المجموع **المخزَّن مؤقتاً** بعد) → ثم `Cache::forget`. فيُحفظ العمود من مجموع قديم يغفل النقاط المُضافة للتو. القراءة الحيّة سليمة، لكن العمود المخزَّن يتخلّف. (`PointService.php:95-98,141-151`)

### 9. حساب سلسلة الأيام (Streak) مكسور على Carbon 3
`calculateStreak` يقارن `$currentDate->diffInDays($dateObj) === $streak`. في Carbon 3 يُرجع `diffInDays` قيمة **عشرية (float)**، فـ `1.0 === 1` (مقارنة صارمة) خطأ، وتنكسر السلسلة فوراً — فإنجازات السلسلة عملياً لا تتقدّم. (`AchievementService.php:127-129`)

### 10. عدم تطابق نموذج/مخطّط لوحة المتصدرين
`Leaderboard` يعرّف `subject_id` و`criteria` في `$fillable`/`$casts` وعلاقة `subject()`، لكن جدول `leaderboards` **لا يحوي هذين العمودين**؛ و`LeaderboardService` يقرأ `criteria` و`subject_id`، فلوحات النوع course أو المبنية على criteria ستفشل/تعود فارغة. كما أن `period_start/period_end` مُلقّمة `datetime` بينما العمود `date`.

### 11. لوحة المتصدرين "قديمة بحكم التصميم"
`updateLeaderboard` يُستدعى فقط من لوحة الأدمن عند الإنشاء/التعديل، ولا يوجد مجدول يعيد حسابها؛ فالترتيب/النقاط تتجمّد حتى يحفظ الأدمن يدوياً (فوق كاش 300 ثانية).

---

## رابعاً: أخطاء بسيطة وأداء

- **N+1 / مسح جداول كامل في كل حدث**: كل `processEvent` يحمّل كل الشارات النشطة ويجري `count()` لكل شارة، ويحمّل كل الإنجازات (مع تحميل كل سجلات إكمال الدروس للسلاسل)، ويمرّ على كل المهام اليومية/الأسبوعية — كل ذلك **تزامنياً** في مسار الطلب. ينبغي نقله إلى Queue/Job. (`BadgeService.php:28-96`, `AchievementService.php:27-63`, `TaskService.php:55-68`)
- **`checkLevelUp` قد يخفض المستوى** ومع ذلك يطلق إشعار "ترقية مستوى" بعد خصم نقاط. (`LevelService.php:37-73`)
- **تكرار محتمل للشارات/الإنجازات تحت التزامن**: فحوص `exists()` غير مدعومة بقيود تفرّد في قاعدة البيانات لهذه الجداول (رغم وجودها على مستوى المخطّط لبعضها — يُنصح بتأكيدها).
- **حقول القواعد غير قابلة للتحرير من الواجهة**: التحقق يسمح بحقول `criteria`/`benefits`/`rewards`/`metadata` لكن **لا نموذج إنشاء/تعديل يعرض مدخلات لها** — فمحرّك المنح التلقائي بلا بيانات ليتصرّف بناءً عليها (فجوة وظيفية في الشارات/الإنجازات/التحديات/المستويات).
- **مسارات `show` معطّلة**: `Route::resource` يولّد `*.show` بلا دوال `show()` مقابلة → أي طلب على `GET badges/{badge}` يُطلق 500. يُنصح بـ `->only([...])`.
- **لا يمكن حذف لوحات المتصدرين**: لا يوجد مسار/دالة `destroy` في مجموعة leaderboards.
- **إمكانية الكتابة على عدّادات داخلية**: `Reward::$fillable` يشمل `quantity_claimed` والمتحكّمات تحفظ عبر `$request->all()`.
- **تفرّد `level_number`** يحسب الصفوف المحذوفة ناعماً، فقد يمنع رقماً مستعاداً.
- **فهرس ناقص على `point_transactions.created_at`** يبطّئ تجميع لوحات المتصدرين الزمنية على جدول كبير.

---

## خامساً: خطة التحسينات المقترحة (حسب الأولوية)

**عاجل (يمنع أعطالاً/استغلالاً):**
1. جعل منح النقاط **idempotent**: تخزين `source_type/source_id` على المعاملة + قيد تفرّد `(user_id, type, source_type, source_id)`، ومنح نقاط الدرس فقط عند أول انتقال. (يغلق الثغرة #1)
2. تحصين `claimReward`: معاملة + قفل صف + قراءة رصيد من القاعدة داخل القفل + قيد تفرّد + زيادة مخزون ذرّية. (#2)
3. إضافة `use App\Models\PointTransaction;` في `LeaderboardService`. (#4 — سطر واحد)
4. تسجيل الأوامر في المجدول: `tasks:reset-daily` و`tasks:reset-weekly`، وأمر جديد لإعادة بناء اللوحات النشطة، وأمر لانتهاء التحديات — يفضَّل قراءة التوقيت من الإعدادات المبذورة أصلاً. (#5، #11)
5. مطابقة مفاتيح إعدادات النقاط مع أنواع الأحداث وإزالة/توصيل `course_completed`. (#6)

**مهم (يستعيد ميزات معطّلة):**
6. توصيل نظام التحديات فعلياً (انضمام + تقدّم + إكمال) أو إزالته. (#3)
7. إعادة تصميم دورية المهام: عمود `period_date` + قيد تفرّد + إعادة تهيئة ذرّية مع مراعاة المنطقة الزمنية. (#5)
8. إصلاح تطابق نموذج/مخطّط اللوحة (`subject_id`/`criteria` وأنواع الـ cast). (#10)
9. إصلاح تطابق مفاتيح "النقاط حسب النوع". (#7)
10. إصلاح مقارنة السلسلة لـ Carbon 3 (تحويل إلى int). (#9)

**تحسينات (جودة/أداء/تجربة):**
11. نقل محرّك المنح إلى Queue/Job وتجميع فحوص الشارات/الإنجازات/المهام لإزالة الـ N+1. (رابعاً)
12. إضافة نماذج تحرير لقواعد `criteria`/`benefits`/`rewards`/`metadata` حتى يعمل المنح التلقائي.
13. تقييد `checkLevelUp` على الترقية فقط والإشعار عند الزيادة الحقيقية. (#8، #12)
14. صلاحيات دقيقة (`permission:manage-gamification`) للأفعال الحسّاسة (خصوصاً `settings.reset` والحذف) بدل الدور الإداري الخام.
15. إضافة `destroy` للوحات، وتقييد مسارات الموارد بـ `->only([...])`، وإزالة `quantity_claimed` من المدخلات القابلة للتعبئة.
16. فهرس `(user_id, created_at)` على `point_transactions`، وأمر تسوية يعيد حساب مجاميع `user_levels` من المعاملات.

---

## ملاحظة منهجية

هذا الفحص قراءةٌ ثابتة للكود (static review) عبر ثلاثة مسارات متوازية (الأدمن، الطالب/المحرّك، البيانات/الجدولة)، وقد تحقّقت يدوياً من أخطر خمس نتائج (زراعة نقاط الدروس، سباق المكافآت، import اللوحة المفقود، عدم جدولة المهام، عدم تطابق مفاتيح الإعدادات) بفحص الأسطر المذكورة مباشرةً. يُنصح بعد تطبيق الإصلاحات بكتابة اختبارات (Pest) لكل من: منع تكرار منح النقاط، صرف المكافأة المتزامن، إعادة تهيئة المهام اليومية، وحساب لوحة أسبوعية.
